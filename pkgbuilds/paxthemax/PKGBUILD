pkgname='paxthemax'
pkgver=999.0.0
pkgrel=1
pkgdesc='System packages installed on paxthemax machines (deprecated)'
arch=('any')
url='https://github.com/paxthemax/arch-repo'
license=('MIT')

package() {
    # Try empty package as we are retiring it
    depends=()
}
